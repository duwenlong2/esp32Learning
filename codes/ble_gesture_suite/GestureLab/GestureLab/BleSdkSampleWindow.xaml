<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GestureLab.BleSdkSampleWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GestureLab BLE SDK 示例">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,Auto,*" Padding="16" RowSpacing="12">
        <TextBlock Text="GestureLab.Ble SDK 示例窗口" FontSize="20" FontWeight="SemiBold" />

        <Grid Grid.Row="1" ColumnDefinitions="Auto,*" ColumnSpacing="12">
            <StackPanel Spacing="6">
                <TextBlock Text="BLE 状态" FontWeight="SemiBold" />
                <TextBlock x:Name="BleStateText" Text="idle" />

                <TextBlock Text="固件状态" FontWeight="SemiBold" Margin="0,8,0,0" />
                <TextBlock x:Name="FirmwareStatusText" Text="-" TextWrapping="Wrap" Width="360" />

                <TextBlock Text="当前手势状态" FontWeight="SemiBold" Margin="0,8,0,0" />
                <TextBlock x:Name="GestureStateText" Text="Idle" />

                <TextBlock Text="最近姿态" FontWeight="SemiBold" Margin="0,8,0,0" />
                <TextBlock x:Name="PoseText" Text="roll=0, pitch=0, yaw=0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="6">
                <TextBlock Text="热键发送状态" FontWeight="SemiBold" />
                <TextBlock x:Name="HotkeyStatusText" Text="未发送" />
                <TextBlock x:Name="HotkeyCountText" Text="发送次数: 0" />
                <TextBlock x:Name="LastHotkeyTimeText" Text="最近发送: -" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="8" Padding="8">
            <ListView
                x:Name="LogList"
                IsItemClickEnabled="False"
                SelectionMode="None"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
        </Border>
    </Grid>
</Window>
